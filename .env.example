BOT_TOKEN=123456:ABC-DEF...

# Оперативный кэш (RAM)
CACHE_TTL_HOURS=12
CACHE_MAX_ENTRIES=1500
CACHE_CLEANUP_INTERVAL_MIN=10
SAVE_MEDIA=true

# SQLite — постоянное хранение (false = только RAM)
DB_ENABLED=false
DB_PATH=data/bot.db
DB_SAVE_MEDIA=false
DB_RETENTION_DAYS=0

# Тексты бота (\\n = перенос строки, HTML разрешён)
# Можно указать файл вместо переменной: TEXT_START_FILE=config/start.txt
# TEXT_START=Привет!\\n\\n{help}
# TEXT_HELP=<b>Мои команды</b>...
# TEXT_PONG=🏓 живой
